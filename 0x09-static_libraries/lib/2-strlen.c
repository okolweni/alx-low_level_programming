#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen -  function that returns the length of a string.
 * @s: the pointer
 *
 * Return: The length of the string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
