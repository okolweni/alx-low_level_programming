#include <stdio.h>

/*
 * main - Entry point
 *
 * Description: program that prints an exact sentence, followed by a new line.
 * 		use the function puts instead of printf.
 *
 * Return: Always 0 (Success)
 */

int main()
{
	puts("\"Programming is like building a multilingual puzzle");

	return 0;
}
