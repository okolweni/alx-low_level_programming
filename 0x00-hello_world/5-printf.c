#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print exact string, followed by a new line.
 * Use the function printf instead of puts.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
